# Pi local auth tokens — never commit (repo is public).
agent/auth.json
agent/sessions

# Dependencies
node_modules/
bun.lock

# Gemini extension runtime artifacts (generated wherever a tool runs)
gemini-output/
# Local Pi runtime state
.atl/
