# Pi local auth tokens — never commit (repo is public).
agent/auth.json
agent/sessions

# Dependencies
node_modules/

# Gemini extension runtime artifacts (generated wherever a tool runs)

# Discord bot local credentials
agent/pi-discord/config.json

# Intercom runtime state
agent/intercom/broker.pid

# Generated deck previews
deck-snapshots/

agent/models-store.json


agent/npm
agent/trust.json

state
