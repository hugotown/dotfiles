{
  "name": "obra-sp-flow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lite deterministic context-extract -> brainstorm -> spec harness for pi (self-contained extension).",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.typecheck.json"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.9",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22",
    "bun-types": "^1.3.14",
    "typescript": "^5"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
