# ~/.config/fish/config.fish
# Manual configuration - User editable
# Philosophy: Nix installs + manages env → User configures behavior in ~/.config
#
# NOTE: Environment variables, PATH, and secrets are managed by Nix in:
#       ~/.fish_env (auto-generated by home-manager, OUTSIDE ~/.config per Exception Rule)

# ===========================================
# Nix-Managed Environment (load FIRST)
# ===========================================

source ~/.fish_env

# ===========================================
# Shell Integrations (auto-generated by Nix)
# ===========================================

source ~/.zoxide.fish
source ~/.atuin.fish
source ~/.yazi.fish
source ~/.cldy.fish

# ===========================================
# User Configuration (add your customizations below)
# ===========================================

# Custom aliases, functions, and settings go here
source ~/.starship.fish
